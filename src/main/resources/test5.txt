def main() {
    float f = 5 / 2.0; // 2.5
    int i = 2 * f; // 5
    if (!"") {
        printf("Zero-length string is false;\n");
    }
    list<float> fl = {};
    if (!fl) {
        printf("Zero-length list is false;\n");
    }
    dict<int, string> isd = {};
    if (!isd) {
        printf("And zero-length dict is also false.\n");
    }
    if ("abc" < "de" && "ef" == "ef") {
        printf("Strings are compared lexicographically.");
    }
}
